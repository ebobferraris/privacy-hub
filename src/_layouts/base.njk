<!DOCTYPE html>
<html lang="{{ lang | default('it') }}">
  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} - {% endif %}{{ 'ui.companyName' | t(lang) }}</title>
  <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
</head>
<body>
  <header class="header">
    <div class="container">
      <h1><a href="{{ '/' | url }}">{{ 'ui.companyName' | t(lang) }}</a></h1>
      <div id="language-dropdown" class="language-dropdown">
        <button class="language-button" onclick="toggleLanguageDropdown()">
          <span class="current-lang" data-translate="currentLang">
            {% if lang == 'it' %}🇮🇹 IT{% elif lang == 'en' %}🇬🇧 EN{% elif lang == 'fr' %}🇫🇷 FR{% elif lang == 'de' %}🇩🇪 DE{% elif lang == 'sl' %}🇸🇮 SL{% else %}🇮🇹 IT{% endif %}
          </span>
          <span class="dropdown-arrow">▼</span>
        </button>
        <div id="language-menu" class="language-menu">
          <a href="{{ '/' | url }}" class="language-item" onclick="changeLanguage('it')">
            🇮🇹 {{ 'languages.it' | t(lang) }} (IT)
          </a>
          <a href="{{ '/en/' | url }}" class="language-item" onclick="changeLanguage('en')">
            🇬🇧 {{ 'languages.en' | t(lang) }} (EN)
          </a>
          <a href="{{ '/fr/' | url }}" class="language-item" onclick="changeLanguage('fr')">
            🇫🇷 {{ 'languages.fr' | t(lang) }} (FR)
          </a>
          <a href="{{ '/de/' | url }}" class="language-item" onclick="changeLanguage('de')">
            🇩🇪 {{ 'languages.de' | t(lang) }} (DE)
          </a>
          <a href="{{ '/sl/' | url }}" class="language-item" onclick="changeLanguage('sl')">
            🇸🇮 {{ 'languages.sl' | t(lang) }} (SL)
          </a>
        </div>
      </div>
    </div>
  </header>

  <main class="main">
    <div class="container">
      {{ content | safe }}
    </div>
  </main>

  <footer class="footer">
    <div class="container">
      <p>&copy; 2025 {{ 'ui.companyName' | t(lang) }}. {{ 'ui.copyright' | t(lang) }}</p>
    </div>
  </footer>

  <script src="{{ '/js/main.js' | url }}"></script>
  <script src="{{ '/js/translations.js' | url }}"></script>
</body>
</html>
