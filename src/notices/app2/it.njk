---
layout: base.njk
title: "App2 - Informativa Privacy (Italiano)"
---

<div class="notice-header">
  <h1>App2 - Informativa Privacy (Italiano)</h1>
  <a href="/" class="back-link">← Torna alla home</a>
</div>

<div class="version-controls">
  <div class="version-selector">
    <label for="version-select">Versione:</label>
    <select id="version-select">
      <option value="latest" selected>Più recente (v1.0)</option>
    </select>
  </div>
</div>

<div class="notice-content">
  <div id="notice-content">
    <h2>Informativa sulla Privacy</h2>
    <p>Questa è l'informativa privacy per App2 in italiano.</p>

    <h3>Raccolta dei Dati</h3>
    <p>Raccogliamo informazioni personali necessarie per fornire i nostri servizi, incluse informazioni di contatto e dati di utilizzo dell'applicazione.</p>

    <h3>Utilizzo dei Dati</h3>
    <p>Utilizziamo i dati raccolti per elaborare le tue richieste, personalizzare l'esperienza utente e mantenere la sicurezza dei nostri sistemi.</p>

    <h3>Protezione dei Dati</h3>
    <p>Implementiamo robuste misure di sicurezza per proteggere le tue informazioni personali da accessi non autorizzati e utilizzi impropri.</p>

    <h3>Conservazione dei Dati</h3>
    <p>Conserviamo i tuoi dati personali solo per il tempo necessario a fornire i nostri servizi e rispettare gli obblighi legali.</p>

    <h3>I Tuoi Diritti</h3>
    <p>Hai pieno diritto di accesso, modifica, cancellazione e portabilità dei tuoi dati personali secondo le normative sulla privacy.</p>

    <h3>Contatti</h3>
    <p>Per esercitare i tuoi diritti o per qualsiasi domanda sulla privacy, puoi contattarci all'indirizzo: privacy@app2.com</p>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Get language from sessionStorage or use langManager
  const savedLang = sessionStorage.getItem('selectedLanguage') || langManager.currentLang;

  // Update all translatable elements
  document.querySelectorAll('[data-translate]').forEach(element => {
    const key = element.getAttribute('data-translate');
    const translation = langManager.getTranslation(key);
    if (translation) {
      element.textContent = translation;
    }
  });

  // Version selector functionality can be added here
  const versionSelect = document.getElementById('version-select');
  versionSelect.addEventListener('change', function(e) {
    console.log('Selected version:', e.target.value);
    // Add version switching logic here
  });
});
</script>
